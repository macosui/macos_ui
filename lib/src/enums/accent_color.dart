/// The macOS accent color which can be changed by the user in *System Settings*
/// → *Apperance* → *Accent color*.
enum AccentColor {
  blue,
  purple,
  pink,
  red,
  orange,
  yellow,
  green,
  graphite,
}
